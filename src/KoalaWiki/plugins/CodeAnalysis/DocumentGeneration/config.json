{
  "schema": 1,
  "type": "completion",
  "description": "根据仓库描述和仓库目录结构，分析仓库的功能和结构，给出目录结构的分析结果",
  "execution_settings": {
    "default": {
      "temperature": 0
    }
  },
  "input_variables": [
    {
      "name": "catalogue",
      "description": "仓库目录结构",
      "required": true
    },
    {
      "name": "readme",
      "description": "仓库默认文档内容",
      "required": true
    },
    {
      "name": "title",
      "description": "当前文档标题",
      "required": true
    }
  ]
}